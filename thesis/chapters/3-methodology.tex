\chapter{Methodology}\label{chap:methodology}

\section{Dataset}

Our dataset is derived from the publicly available \acs{FRLL}~\cite{frll} set, comprising 102 full-color, $1350 \times 1350$ frontal, \acs{ICAO}-compliant facial images. Self-reported age, gender and ethnicity are included. Attractiveness ratings (on a 1--7 scale from ``much less attractive than average'' to ``much more attractive than average'') for the faces from 2513 people (ages 17--90) are included as well. The \acs{FRLL} dataset was selected for its high-fidelity \acs{ICAO}-compliant images, controlled acquisition conditions, and extensive demographic annotations, which are crucial for analyzing perceptual quality and potential demographic biases in subjective ratings. Each image was encoded using four printer-proof steganographic methods, each applied at nine different intensity levels, $x = 0.6 + 0.1k$ for $k \in \{0,\dots,8\}$, yielding a total of 3,672 distorted images.

\begin{table}[htbp]
\centering
\caption{Partitioning of the steganographically distorted \acs{FRLL} dataset.}
\label{tab:frll_dataset_partition}
\begin{tabular}{lccc}
\hline
\textbf{Subset}     & \textbf{Subjects} & \textbf{Images} & \textbf{Purpose} \\
\hline
\acs{MOS} train set & 12                & 432             & Train \acs{FR} fusion model \\
\acs{MOS} test set  & 3                 & 108             & Final evaluation  \\
\acs{pMOS} set      & 87                & 3{,}132           & Generate proxy \acs{MOS} labels \\
\acs{NR} train set  & 99                & 3{,}564           & \acs{MOS} train + \acs{pMOS} \\
\hline
\textbf{Total}      & \textbf{102}      & \textbf{3{,}672}  & --- \\
\hline
\end{tabular}
\end{table}

The dataset was partitioned to support both supervised learning and independent evaluation. A small, demographically diverse subset of 15 identities, shown in~\autoref{fig:frll_mos_set} was annotated with \acs{MOS} through subjective tests. This set was split into a training portion (12 identities,~\autoref{fig:frll_mos_train_set}) used to fit the fusion model, and a disjoint test portion (3 identities,~\autoref{fig:frll_mos_test_set}) held out for final evaluation. The remaining 87 identities formed the unlabeled set, to which \acf{pMOS} were later assigned. Together, the annotated and pseudo-labeled subsets were used to train the \acs{NR-IQA} model. A summary of these partitions, including the number of subjects and images per subset, is presented in~\autoref{tab:frll_dataset_partition}.

\begin{figure}[htbp]
    \centering
    \begin{subfigure}{0.76\textwidth}
        
        \centering
        \includegraphics[width=\textwidth]{images/frll/mos_train_set.pdf}\\
        \caption{\acs{MOS} train set}\label{fig:frll_mos_train_set}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.19\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/frll/mos_test_set.pdf}\\
        \caption{\acs{MOS} test set}\label{fig:frll_mos_test_set}
    \end{subfigure}
    \caption{Reference images from the \acs{MOS} set, comprising the selected subjects from the \acs{FRLL} dataset.}\label{fig:frll_mos_set}
\end{figure}

The steganographic distortions are visible in~\autoref{fig:steganography}, which shows examples of distorted facial images from each method. The distortions are applied to the original images, and the resulting stego images are used for both subjective evaluation and training of the \acs{NR-IQA} model. The selected steganographic methods represent diverse classes of distortion processes (geometric, color-based, local texture perturbation) commonly encountered in practical steganographic applications. The chosen intensity levels ensure a broad perceptual range from barely noticeable to clearly visible distortions.

\begin{figure}[htbp]
    \centering
    \begin{subfigure}{0.23\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/frll/005_StegaStamp_1.4.jpg}\\
        \caption{StegaStamp}\label{fig:stegastamp}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.23\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/frll/005_CodeFace_1.4.jpg}\\
        \caption{Code\,Face}\label{fig:codeface}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.23\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/frll/005_RiemStega_1.4.jpg}\\
        \caption{RiemStega}\label{fig:riemstega}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.23\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/frll/005_StampOne_1.4.jpg}\\
        \caption{StampOne}\label{fig:stampone}
    \end{subfigure}
    \caption{Steganographically distorted facial images from each method.}\label{fig:steganography}
\end{figure}

\section{Collection of Subjective Scores}

We followed the \acs{ITU-R BT.500-15}~\cite{itu2023r} recommendation and adopted the \acl{SS} method. The test was implemented using a custom Django~\cite{django} web application seen in~\autoref{fig:webapp}. Prior to the test session, participants signed an informed consent form and filled out a registration form providing demographic and environmental information such as age, gender, education level, country of origin, ethnicity, type of device being used, and where they were located at the time of the test session. After this, observers were shown 10 randomly selected sample images. These examples were designed to help them understand the variation in quality across the dataset.

In the subjective assessment phase, each image was shown individually, with no time limit. Ratings were submitted using a labeled slider, and automatic saving ensured session robustness.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.5\textwidth]{images/webapp/test.png}
    \caption{Django-based web application created for the Single Stimulus test sessions.}\label{fig:webapp}
\end{figure}

Each image in the \acs{MOS} set was evaluated approximately 30 times by human observers, resulting in over 14,000 ratings. We had around 200 participants, each session lasted about 22 minutes and included roughly 70 evaluations.~\autoref{tab:demographics} summarizes the demographic profile of the participant pool. Following the session, outlier observers were identified and removed using both Kurtosis-based and correlation-based post-screening methods described in \acs{ITU-R BT.500-15}, resulting in the exclusion of one participant.

\begin{table}[htbp]
    \centering
    \caption{Demographic and non-demographic profile of the observers.}
    \label{tab:demographics}

    % First row of subtables
    \begin{subtable}{0.3\textwidth}
        \centering
        \caption{Age group}
        \begin{tabular}{lr}
            \hline
            \textbf{Age} & \textbf{Count} \\
            \hline
            18--25 & 85 \\
            26--40 & 54 \\
            41--60 & 52 \\
            Above 60 & 9 \\
            Under 18 & 2 \\
            \hline
        \end{tabular}
    \end{subtable}
    \hfill
    \begin{subtable}{0.3\textwidth}
        \centering
        \caption{Gender}
        \begin{tabular}{lr}
            \hline
            \textbf{Gender} & \textbf{Count} \\
            \hline
            Male & 143 \\
            Female & 59 \\
            \hline
        \end{tabular}
    \end{subtable}
    \hfill
    \begin{subtable}{0.3\textwidth}
        \centering
        \caption{Ethnicity}
        \begin{tabular}{lr}
            \hline
            \textbf{Ethnicity} & \textbf{Count} \\
            \hline
            White & 177 \\
            Latino & 8 \\
            Black & 6 \\
            Multiracial & 3 \\
            Other & 7 \\
            \hline
        \end{tabular}
    \end{subtable}

    \vfill

    \begin{subtable}{0.3\textwidth}
        \centering
        \caption{Education Level}
        \begin{tabular}{lr}
            \hline
            \textbf{Education} & \textbf{Count} \\
            \hline
            Bachelor's & 92 \\
            Master's & 63 \\
            Doctorate & 29 \\
            High School & 14 \\
            Other & 4 \\
            \hline
        \end{tabular}
    \end{subtable}
    \hfill
    \begin{subtable}{0.3\textwidth}
        \centering
        \caption{Country of Origin}
        \begin{tabular}{lr}
            \hline
            \textbf{Country} & \textbf{Count} \\
            \hline
            Portugal & 170 \\
            Brazil & 16 \\
            Russia & 3  \\
            India & 2 \\
            Ghana & 2 \\
            Others & 9 \\
            \hline
        \end{tabular}
    \end{subtable}
    \hfill
    \begin{subtable}{0.3\textwidth}
        \centering
        \caption{Device Used}
        \begin{tabular}{lr}
            \hline
            \textbf{Device} & \textbf{Count} \\
            \hline
            Laptop & 94 \\
            Phone & 65 \\
            Desktop & 42 \\
            Other & 1 \\
            \hline
        \end{tabular}
    \end{subtable}
\end{table}

The database was implemented using the Django web framework, which provides an \acf{ORM} layer that directly maps Python model classes to database tables. The \acs{ORM} also enforces data integrity constraints and simplifies querying for statistical analysis. The database management system used was PostgreSQL.

\begin{figure}[htbp]
    \centering
    \begin{subfigure}{0.8\textwidth}
        \includegraphics[width=\textwidth]{images/webapp/db_conceptual.png}
        \caption{Conceptual schema.}\label{fig:db_conceptual}
    \end{subfigure}

    \par\vspace{1.5em}
    \begin{subfigure}{0.8\textwidth}
        \includegraphics[width=\textwidth]{images/webapp/db_physical.png}
        \caption{Physical schema.}\label{fig:db_physical}
    \end{subfigure}

    \caption{Conceptual and physical database schemas implemented in the Django web application.}
    \label{fig:db_schemas}
\end{figure}


The database schema was designed to ensure structured storage and traceability of all subjective quality evaluation data.~\autoref{fig:db_conceptual} presents the conceptual data model, which defines the main entities and their relationships. The profile entity stores participant metadata, including age, gender, education, ethnicity, country of origin, and device used. The session models individual tests and is linked to both the participant profile and the dataset being evaluated. The image entity encodes each image's filename, associated distortion type, and distortion level. The test entity stores individual subjective ratings, including the precise timestamp of submission, linked to both the session and the image presented. An auxiliary feedback entity captures optional, anonymous, observer comments and critiques.

The physical implementation of this schema, shown in~\autoref{fig:db_physical}, is realized as a relational database with explicit foreign key constraints to ensure referential integrity. The dataset and profile fields in session formally enforce the linkage to specific datasets and participants. The test table connects each subjective rating to its session and image, enabling consistent aggregation of ratings into a statistical descriptor such as \acs{MOS} and confidence intervals per image and per distortion level. The relational model also facilitates efficient querying for downstream statistical analysis and supports reproducibility of the experimental protocol in line with \acs{ITU-R BT.500-15} recommendations.

\section{Debiasing Subjective Scores}

To correct for demographic bias in the subjective scores, we followed a procedure inspired by prior work on bias correction in perceptual tasks~\cite{ols}, where we applied a residualization method based on linear modeling. An \acf{OLS} regression was fit to the \acs{MOS} values, using observer and image attributes, and their pairwise interactions as categorical predictors. The fitted bias components were subtracted from the original scores, and the residuals were mean-centered to preserve the global score distribution. As shown in~\autoref{tab:anova}, several factors exhibit statistically significant effects on the \acs{MOS} prior to residualization, notably observer and subject ethnicity. After applying the residualization procedure, these effects disappear, as confirmed by an \acf{ANOVA}~\cite{anova} and \acf{mANOVA}~\cite{manova} test, showing no significant impact from any individual factor. The corrected \acs{MOS} labels are then used as ground truth in all supervised stages of the pipeline to ensure fairness and reduce the influence of socially conditioned priors.

\begin{table}[htbp]
    \centering
    \caption{\acs{ANOVA} and \acs{mANOVA} results for observer and image attributes. Before debiasing, several factors show statistically significant effects on \acs{MOS}, $p$-value $< 0.05$.\@ After residualization, all main effects show no significant impact, confirming the effectiveness of the debiasing procedure.}\label{tab:anova}
    \begin{tabular}{lcc}
        \hline % chktex 44
        \textbf{Factor} & $\mathbf{p}$-\textbf{value} & $\mathbf{p}$-\textbf{value (residualized)} \\
        \hline % chktex 44
        Subject attractiveness                      & 0.3173               & 0.3173 \\
        Observer gender                             & 0.022                 & 0.9930 \\
        Observer ethnicity                          & $8.44 \times 10^{-4}$ & 1 \\
        Subject gender                              & $1.60 \times 10^{-3}$ & 0.9722 \\
        Subject ethnicity                           & $7.36 \times 10^{-3}$ & 1 \\
        Observer gender $\times$ Subject gender     & 0.6417              & 0.6417 \\
        Observer ethnicity $\times$ Subject ethnicity & 0.0582              & 0.0582 \\
        \hline % chktex 44
    \end{tabular}
\end{table}

\section{Evaluation of \acl{FR} Metrics}

We compute 40 \acs{FR-IQA} scores for each distorted image and compare them against the corresponding \acs{MOS}. To assess alignment with human perception, we evaluate both linear and monotonic relationships using \acs{PLCC} and \acs{SRCC}. These measures provide a ranking of metrics, IQA score, by their mean correlation. Negative correlations are kept as is, \acs{PLCC} tests linear fit, while \acs{SRCC} tests monotone ordering. Adding magnitudes would treat them as interchangeable, thus, rewarding inconsistent behavior. If a metric's polarity is reversed, the downstream fusion model can learn an appropriate sign or transformation during training.

Beyond pairwise correlation, we analyze the intrinsic dimensionality of the \acs{FR} feature space using \acf{SVD}. Cumulative variance helps to choose the number of retained metrics $k$ at a target threshold, and the Picard plot~\cite{picard} validates stability by contrasting singular values with the projected response. This analysis yields a compact subset of normalized, polarity-aligned metrics, which will form the input to the fusion stage.

\section{Full-Reference Fusion Metric}

Building on the selected \acs{FR-IQA} features, we construct a fusion-based metric that learns to predict perceptual quality directly from heterogeneous objective scores. Each distorted image is represented by its $k$-dimensional feature vector, and the corresponding human \acs{MOS} serves as the regression target.

A diverse set of regressors is trained on these features:

\begin{itemize}
    \item Linear: Linear Regression~\cite{linearregression}, Ridge~\cite{ridgeregression}, Bayesian Ridge~\cite{bayesianridge}, ElasticNet~\cite{elasticnet}.
    \item Ensembles: Random Forest~\cite{randomforest}, Extra Trees~\cite{ensembles}, Gradient Boosting~\cite{gradboost}, Histogram Gradient Boosting~\cite{histogram}.
    \item Boosted trees: \acs{XGBoost}~\cite{xgboost}, \acs{LightGBM}~\cite{lightgbm}, \acs{CatBoost}~\cite{catboost}.
\end{itemize}

Each regressor is tuned with a \acf{TPE}~\cite{tpe} search implemented in Optuna~\cite{optuna}, using Successive Halving~\cite{pruner} for early stopping, and evaluated by 5-fold cross-validation. Models are ranked by the mean of \acs{PLCC} and \acs{SRCC} between predictions and \acs{MOS}. Per-model outputs include predictions, scores, tuned parameters, and trained checkpoints.

For tree-based models we compute \acf{SHAP}~\cite{shap}, highlighting which \acs{FR} metrics drive the fused prediction and whether their contributions are consistent across the dataset. The result is a fusion-based \acs{FR} metric that consistently outperforms any single score and serves as both a standalone contribution and the basis for pseudo-label generation.

\section{\acl{pMOS} Generation}

The best validated fusion model is then applied to the unlabeled portion of the distorted dataset to produce \acs{pMOS} labels. Each image is passed through the selected top-$k$ metrics, normalized with the same scaler used in training, and scored by the fusion model.

This procedure extends perceptual supervision from the small human-annotated subset to the entire dataset, producing a complete set of labels for downstream use. These pseudo labels enable two main objectives:

The first objective is \acs{FR-IQA} at scale when human \acs{MOS} is scarce. When a pristine reference is available but exhaustive subjective scoring is not feasible, the fusion metric provides \acs{pMOS} for all distorted versions of a reference image. This makes it possible to analyze distortion sensitivity curves, compare distortion methods across payload levels, and generate robust statistical summaries without collecting new subjective scores. In practice, this establishes a reproducible way to extend human perception to large datasets with minimal annotation effort.

The second objective is supervision for training \acs{NR-IQA} models. With a large quantity of labeled images available, we can train \acs{NR} regressors that predict perceptual quality directly from distorted images without a reference. The pseudo labels provide the scale and coverage needed to fit modern deep backbones, and the resulting models operate in fully blind conditions, enabling quality assessment of images where no reference is available.

\section{\acs{NR} Metric from \acs{FR} Fusion}\label{sec:3nr-model}

We train on two complementary image sets: a labeled set containing the $\approx$10\% of images annotated with human \acs{MOS}, and a pseudo labeled set with the remaining distorted images scored by our \acs{FR} fusion into \acs{pMOS}.

We train three pretrained backbones with a 1-unit linear regressor head using Smooth-$L_1$ loss ($\beta\!=\!1$), AdamW~\cite{adamw}, cosine learning-rate scheduling, batch size of 31, mixed-precision on GPU, and ImageNet normalization.~\autoref{tab:backbone_config} summarizes the training configuration. There was no use of data augmentation algorithms, since the distortions themselves provide sufficient variability.

\begin{table}[htbp]
    \centering
    \caption{Training configuration of pretrained backbones for \acs{NR} metric learning.}
    \label{tab:backbone_config}
    \begin{tabular}{lccccc}
        \hline
        \textbf{Backbone} & \textbf{Input} & \textbf{Epochs} & \textbf{Learning rate} & \textbf{Weight decay} & \textbf{Drop rate} \\
        \hline
        ResNet-50~\cite{resnet} & $224^{2}$ & 50 & $1\times 10^{-3}$ & $1\times 10^{-4}$ & 0 \\
        EfficientNet-B1~\cite{efficientnet} & $240^{2}$ & 60  & $1\times 10^{-3}$ & $1\times 10^{-5}$ & 0.2 \\
        ViT-B/16~\cite{vit} & $224^{2}$ & 60 & $3\times 10^{-4}$ & 0.05 & 0.1 \\
        \hline
    \end{tabular}
\end{table}

We adopt 5-fold \acf{CV}. The merged dataset is split into 5 disjoint folds $\{F_1,\dots,F_5\}$ at the image level, stratification not enforced. For split $i$, training uses $D_{\text{train}}=D\setminus F_i$ and validation uses $D_{\text{val}}=F_i$. All hyperparameters are kept fixed. We monitor validation \acs{MAE} after every epoch and apply early stopping with patience $p$ epochs. For each fold we retain the checkpoint with the lowest validation \acs{MAE}. Let $m_i$ denote a metric on fold $i$, we report the mean and dispersion across the 5 folds:

\begin{equation}
    \bar{m}=\frac{1}{5}\sum_{i=1}^{5} m_i,\quad
    \hat{\sigma}=\sqrt{\frac{1}{4}\sum_{i=1}^{5}(m_i-\bar{m})^2},\quad
    \text{CI}_{95}:\;\bar{m}\pm1.96\,\frac{\hat{\sigma}}{\sqrt{5}}.
\end{equation}

Final test set results are reported on a disjoint \acs{MOS}-only set. Full hardware and software environment details are in~\autoref{app:env}.
