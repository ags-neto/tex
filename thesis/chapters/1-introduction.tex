\chapter{Introduction}\label{chap:introduction}

\section{Contextualization}\label{sec:contextualization}

Image quality refers to the degree to which a visual representation meets perceptual or functional expectations. It is associated with the presence or absence of distortions, artifacts, or degradations that influence how images are perceived by humans or processed by machines. High-quality images preserve structural, textural, and color information in a way that aligns with human visual preferences and supports reliable computer vision performance~\cite{survey2019shahrukh, ma2025survey}.

\acf{IQA} is the process of quantifying image quality in a systematic and reproducible manner. It plays a central role in applications such as image acquisition~\cite{survey2017aquisition}, denoising~\cite{survey2023denoising}, compression~\cite{rabbani1991digital, survey2025compression}, transmission~\cite{chandra2016transmission, survey2019transmission}, and restoration~\cite{survey2025restoration}. Surveys and comparative studies~\cite{survey2019shahrukh, survey2006wang, survey2009thung, survey2020zhai} highlight the diversity of approaches, ranging from signal-based models to data-driven learning techniques, reflecting the complexity of modeling human visual perception.

\acs{IQA} methods are generally divided into subjective and objective categories. Subjective assessment relies on human observers, typically following standardized protocols such as \acf{ITU-R BT.500-15}~\cite{itu2023r}, and provides the most direct alignment with perceptual quality. However, it is costly, time-consuming, and not scalable. Objective methods, by contrast, employ computational models to automatically estimate quality, offering scalability and repeatability. Their effectiveness depends on how well they approximate subjective judgments, a long-standing challenge documented in \acs{IQA} benchmarks and reviews~\cite{survey2006wang}.

Within objective \acs{IQA}, three main categories are distinguished: \acf{FR}~\cite{survey2019shahrukh}, \acf{NR}~\cite{kamble2015nriqa}, and \acf{RR}~\cite{dost2022rriqa}. \acs{FR} approaches assume access to pristine reference images, \acs{RR} methods use partial reference information, and \acs{NR} approaches attempt to predict quality without any reference, making them the most challenging and increasingly important for real-world scenarios.

A specialized subfield, \acf{FIQA}, addresses the assessment of face images with respect to both perceptual fidelity and biometric utility~\cite{survey2022fiqa}. High-quality facial images are essential for accurate recognition in security, forensics, and surveillance. Unlike general \acs{IQA}, \acs{FIQA} must account for unique challenges such as pose, occlusion, and demographic variability~\cite{abed2025fiqabiometric, survey2017fiqaattack}. This reflects a broader trend in \acs{IQA} research toward domain-specific assessment strategies~\cite{survey2020zhai}.

The \acf{ICAO}~\cite{icao2015} and the \acf{ISO} / \acf{IEC} 29794--5 standard~\cite{iso2010iec} establish guidelines for image quality in \acf{MRTDs}. In this context, the \acf{OFIQ} is the reference implementation for the \acs{ISO} / \acs{IEC} 29794--5  international standard. These guidelines ensure uniform image conditions (e.g., lighting, focus, and resolution) and consistency across datasets. While these regulations establish a technical baseline, they do not account for perceptual biases and demographic variability in \acs{FIQA}.

\acs{FIQA} systems have been shown to produce different quality scores depending on a person's age, gender, or skin tone~\cite{babnik2022bias, terhorst2020bias, babnik2022bias}. These differences often happen because the training data includes more examples from certain groups and fewer from others. For example, images of people with darker skin or non-frontal poses are often less common in publicly available datasets, which leads to lower quality scores for those individuals~\cite{kanwisher2006fusiform}. This can cause face recognition systems to perform worse for some groups than others, raising serious concerns about fairness.

The ethical implications of these biases are profound. Political regulations, such as the European Convention on Human Rights (Article 14)~\cite{eu1950art14}, the Universal Declaration of Human Rights (Article 7)~\cite{un1947art7}, the General Data Protection Regulation (Article 22)~\cite{eu2016art22}, and emerging AI governance frameworks, such as the European Artificial Intelligence Act (2024)~\cite{eu2024ai} and proposals in the Artificial Intelligence Civil Rights Act (2024) of the U.S. Congress~\cite{us2024ai}, aim to prevent discriminatory decisions. Despite these efforts, biases persist, often introduced through the human observers who evaluate facial images for \acs{FIQA} algorithms.

Evidence from neuroscience further supports the complexity of facial image perception. The fusiform face area, a specialized region in the human brain, is selectively activated by face stimuli~\cite{kanwisher2006fusiform, tsao2008mechanisms}. This biological specialization makes \acs{FIQA} particularly sensitive to both stimulus features (e.g., age, gender, ethnicity, attractiveness) and the demographic background of the observers.

An even greater challenge arises when evaluating the quality of steganographically distorted facial images. Steganography is the practice of concealing information within digital media, typically by subtly modifying pixel values in a way that is imperceptible to human observers~\cite{morkel2005steganography}. Although visually unobtrusive, these alterations can degrade biometric features and compromise recognition performance. \acf{NR-IQA} approaches, while not requiring references, are generally not designed to detect such imperceptible, task-relevant distortions.

This issue is especially relevant for \acs{MRTDs}, where enhanced security is critical. Incorporating steganographic imagery, such as hidden tamper-evident or authentication data, helps protect against increasingly sophisticated threats to identity documents. The necessity for such measures is underscored by growing criminal activity involving IDs. Reports from the European Border and Coast Guard Agency consistently highlight document fraud as one of the most serious threats at EU external borders~\cite{frontex2023risk}. Similarly, Europol identifies fraudulent IDs as a key enabler of organized crime and migrant smuggling~\cite{europol2022crime}. INTERPOL's SLTD database currently contains more than 138 million records of lost or stolen travel documents, illustrating the global scale of the problem~\cite{interpol2023sltd}. In the United States alone, identity fraud losses reached an estimated \$43 billion in 2023~\cite{javelin2023fraud}.

Steganographically embedding cryptographic signals in passport portraits can therefore serve as a complementary line of defense alongside chip-based authentication. This approach directly addresses known threats such as morphing attacks~\cite{frontex2019morphing}, photo substitution~\cite{icao2015}, and stolen-document misuse~\cite{interpol2023sltd}, while aligning with \acs{ICAO}'s emphasis on secure and standardized travel document images~\cite{icao2015}.

\section{Problem Statement}\label{sec:problem_statement}

Despite substantial advances in \acs{FIQA}, existing methods are not designed to capture subtle but security-critical degradations, particularly those introduced by steganographic embedding. Steganography alters pixel statistics in ways that are imperceptible to human observers yet capable of undermining biometric feature extraction. Conventional \acs{FIQA} approaches, whether based on handcrafted features~\cite{henniger2020assessment}, supervised learning from quality annotations~\cite{hernandez2019faceqnet, meng2021magface, terhorst2020serfiq}, or ranking-based formulations~\cite{liu2017rankiqa}, are typically trained on distortion categories represented in standard datasets (e.g., blur, noise, compression). Consequently, they fail to generalize to covert perturbations that do not visibly degrade an image but can critically impair recognition accuracy.

A second limitation lies in fairness. Empirical studies show that \acs{FIQA} models often inherit demographic biases from their training data, leading to systematically lower quality scores for specific subgroups~\cite{babnik2022bias}. In high-stakes applications such as \acs{MRTDs}, governed by \acs{ICAO} and \acs{ISO} / \acs{IEC} standards~\cite{icao2015, iso2010iec}, these disparities raise profound ethical and legal concerns. At the same time, evidence from border security agencies highlights that document fraud, morphing attacks, and illicit ID use remain prevalent, underscoring the necessity of robust and equitable quality control mechanisms.

This motivates the development of no-reference \acs{FIQA} methods explicitly sensitive to both perceptual fidelity and biometric utility in the presence of steganographic distortions. To address this gap, we propose a framework that fuses multiple full-reference metrics into a proxy ground truth aligned with subjective quality judgments. This fused metric supervises the learning of a \acs{NR}-\acs{FIQA} model tailored to steganographically modified facial images. By embedding considerations of task relevance, perceptual validity, and demographic fairness, our approach aims to provide a principled bridge between human perception, biometric system requirements, and the operational security demands of \acs{MRTDs}.















% NEED TO READ THIS

% \chapter{Introduction}\label{chap:introduction}

% \section{Contextualization}\label{sec:contextualization}

% Image quality refers to the degree to which a visual representation meets perceptual or functional expectations. It reflects the presence or absence of distortions, artifacts, or degradations that influence how images are perceived by humans or processed by machines. High-quality images preserve structural, textural, and color information in a way that aligns with human visual preferences and supports reliable computer vision performance~\cite{survey2019shahrukh, ma2025survey}.

% \acf{IQA} seeks to quantify these notions of quality in a systematic and reproducible way. It plays a critical role in applications such as image acquisition~\cite{survey2017aquisition}, denoising~\cite{survey2023denoising}, compression~\cite{rabbani1991digital, survey2025compression}, transmission~\cite{chandra2016transmission, survey2019transmission}, and restoration~\cite{survey2025restoration}. Surveys and comparative studies~\cite{survey2019shahrukh, survey2006wang, survey2009thung, survey2020zhai} reveal the diversity of existing approaches, ranging from signal-based heuristics to data-driven learning, and highlight the difficulty of modeling human visual perception.

% Two broad methodological paradigms dominate \acs{IQA}: subjective and objective. Subjective assessment, following protocols such as \acf{ITU-R BT.500-15}~\cite{itu2023r}, remains the gold standard because it directly reflects human perception. Yet, it is costly, time-consuming, and impractical at scale. Objective methods offer scalability and repeatability by automating the estimation process. Their challenge lies in how closely they can approximate subjective judgments, a problem still unresolved despite decades of research~\cite{survey2006wang}.

% Objective approaches are further classified as full-reference (FR)~\cite{survey2019shahrukh}, reduced-reference (RR)~\cite{dost2022rriqa}, and no-reference (NR)~\cite{kamble2015nriqa}. FR methods assume access to pristine references, RR exploit partial information, and NR attempt the most difficult task, predicting quality without any reference image. NR methods are of particular importance in real-world deployments, where reference data are rarely available.

% Within this landscape, \acf{FIQA} focuses on facial images, with the goal of quantifying not only perceptual fidelity but also biometric utility~\cite{survey2022fiqa}. Applications range from identity verification in border control to surveillance and forensics. Unlike general IQA, FIQA must handle challenges such as pose, occlusion, and demographic variability~\cite{abed2025fiqabiometric, survey2017fiqaattack}. This reflects a broader shift toward domain-specific IQA, in which quality is evaluated relative to the task at hand~\cite{survey2020zhai}.

% International regulations codify technical baselines for biometric images. The \acf{ICAO}~\cite{icao2015} and the \acf{ISO}/\acf{IEC} 19794--5 standard~\cite{iso2010iec} prescribe requirements on lighting, pose, and resolution for \acf{MRTDs}. These ensure uniform capture conditions but do not address perceptual bias or demographic fairness.

% Indeed, FIQA algorithms have been shown to assign systematically lower quality scores to certain demographic groups~\cite{babnik2022bias, terhorst2020bias}. Darker skin tones, non-frontal poses, and female faces often receive poorer evaluations, largely due to imbalanced training data. This creates downstream risks: recognition performance becomes inconsistent, and vulnerable populations bear disproportionate errors. In high-stakes applications such as MRTDs, this inequity is not only technical but ethical, directly challenging principles of fairness.

% Regulatory efforts seek to address these issues. Frameworks such as the European Convention on Human Rights (Article 14)~\cite{eu1950art14}, the GDPR (Article 22)~\cite{eu2016art22}, the AI Act (2024)~\cite{eu2024ai}, and U.S. legislative proposals~\cite{us2024ai} establish non-discrimination as a requirement for automated decision-making. Yet such policies cannot guarantee fairness if the underlying quality assessments themselves are biased. Since most FIQA systems ultimately inherit human labels, they also inherit human biases.

% From a perceptual perspective, neuroscience shows that face processing engages specialized cortical areas such as the fusiform face area~\cite{kanwisher2006fusiform, tsao2008mechanisms}. This biological specialization underscores why faces are perceived differently depending on both stimulus attributes (age, gender, attractiveness) and observer demographics. These findings reinforce the difficulty of developing universally fair FIQA models.

% A further, largely unexplored challenge is the assessment of steganographically distorted facial images. Steganography conceals information within images by minimally altering pixel values~\cite{morkel2005steganography}. Such modifications are imperceptible but can degrade biometric feature extraction. This creates a paradox: images may appear visually flawless to humans yet compromise recognition accuracy. Conventional NR-IQA methods are ill-suited to capture this type of distortion because they rely on visible degradations such as blur or compression.

% This challenge is especially pressing for MRTDs. Fraud involving manipulated or stolen documents remains a top concern for agencies such as Frontex~\cite{frontex2023risk}, Europol~\cite{europol2022crime}, and INTERPOL~\cite{interpol2023sltd}. In 2023, identity fraud alone caused losses exceeding \$40 billion in the United States~\cite{javelin2023fraud}. Steganographic embedding of cryptographic markers in facial portraits provides an additional layer of defense against morphing~\cite{frontex2019morphing}, substitution~\cite{icao2015}, and other attacks. Yet without reliable quality assessment tailored to these distortions, the effectiveness of such security measures is undermined.

% \section{Problem Statement}\label{sec:problem_statement}

% Despite significant advances in FIQA, current methods are not designed to detect subtle but security-critical degradations introduced by steganography. Pixel-level perturbations imperceptible to humans can nonetheless impair biometric feature extraction. Traditional FIQA methods, whether handcrafted~\cite{henniger2020biosig}, supervised~\cite{hernandez2019faceqnet, meng2021magface, terhorst2020serfiq}, or ranking-based~\cite{liu2017rankiqa}, are trained on conventional distortions (blur, noise, compression) and fail to generalize to covert, task-relevant perturbations.

% A second limitation lies in fairness. FIQA models systematically reflect demographic imbalances in their training sets, producing disparate quality scores for certain groups~\cite{babnik2022bias}. For MRTDs, where ICAO and ISO/IEC standards define technical baselines~\cite{icao2015, iso2010iec}, such disparities raise both ethical and legal concerns. Quality control that disadvantages specific subpopulations directly undermines the legitimacy of biometric security infrastructures.

% Together, these limitations reveal a critical gap. What is needed is a \textbf{no-reference FIQA framework} capable of:
% \begin{enumerate}
%     \item Detecting imperceptible but recognition-relevant degradations, such as those introduced by steganography.
%     \item Aligning predictions with perceptual judgments while mitigating demographic bias.
%     \item Operating without reference images, to match the operational reality of border control and ID verification.
% \end{enumerate}

% This thesis addresses this gap through a \textbf{Full-to-No-Reference framework}. We first fuse multiple FR-IQA metrics into a proxy ground truth aligned with subjective judgments. These pseudo-MOS labels then supervise a NR-FIQA model trained specifically on steganographically modified facial images. By embedding considerations of perceptual validity, biometric utility, and demographic fairness, this framework aims to bridge human perception, system requirements, and operational security in MRTDs.


















% \chapter{Introduction}\label{chap:introduction}

% \section{Contextualization}\label{sec:contextualization}

% Image quality refers to the extent to which a visual representation satisfies perceptual and functional expectations. It depends on the presence or absence of distortions that influence both human perception and machine processing. High-quality images preserve structural, textural, and color information, supporting human visual comfort as well as reliable computer vision performance~\cite{survey2019shahrukh, ma2025survey}.

% \ac{IQA} (\acs{IQA}) is the systematic quantification of image quality. It underpins applications such as image acquisition~\cite{ciancio2010no}, denoising~\cite{dabov2007image}, compression~\cite{rabbani1991digital}, transmission~\cite{engelke2010wireless}, and restoration~\cite{survey2025restoration}. Over the past two decades, \acs{IQA} research has advanced from signal fidelity models toward perceptual similarity metrics and, more recently, data-driven learning approaches~\cite{survey2006wang, survey2020zhai}. This reflects the inherent difficulty of approximating human visual judgments computationally.

% \acs{IQA} methods are generally divided into subjective and objective categories. Subjective assessment, guided by standards such as ITU-R BT.500~\cite{itu2023r}, remains the gold standard but is costly and unscalable. Objective assessment offers automation and reproducibility, with methods typically categorized into full-reference (FR), reduced-reference (RR), and no-reference (NR)~\cite{survey2019shahrukh}. FR assumes access to pristine references, RR relies on partial side information, and NR predicts quality without references. Among these, NR is the most difficult yet also the most relevant for real-world deployment.

% A specialized subfield, Facial Image Quality Assessment (FIQA), evaluates face images in terms of both perceptual fidelity and biometric utility~\cite{survey2022fiqa}. High-quality facial images are critical for reliable recognition in border control, forensics, and surveillance. Compared to generic \acs{IQA}, \acs{FIQA} must contend with unique challenges such as pose variation, occlusion, and demographic diversity~\cite{terhorst2020serfiq}. International standards, including ICAO~\cite{icao2015} and ISO/IEC 19794--5~\cite{iso2010iec}, establish baseline requirements for image capture in Machine-Readable Travel Documents (MRTDs). However, these standards do not address perceptual bias or fairness across demographic groups.

% Indeed, \acs{FIQA} algorithms frequently exhibit systematic demographic disparities~\cite{babnik2022bias, terhorst2020bias}. Scores can vary with age, gender, or skin tone due to unbalanced training data, leading to degraded recognition performance for underrepresented groups. This raises significant ethical and legal concerns, particularly in regulated domains such as border security, where fairness is explicitly mandated by human rights law and emerging AI governance frameworks~\cite{eu2016art22, eu2024ai}.

% Beyond fairness, \acs{FIQA} faces another underexplored challenge: steganographically distorted facial images. Steganography embeds information within images by subtly modifying pixel values, typically in ways imperceptible to humans~\cite{morkel2005steganography}. While visually unobtrusive, these modifications can disrupt biometric features and compromise recognition. This problem is particularly relevant in MRTDs, where steganographic embedding of cryptographic signals has been proposed as a defense against document fraud, morphing attacks, and identity theft~\cite{frontex2019morphing, interpol2023sltd}. Reports from border agencies confirm the scale of the threat: the INTERPOL SLTD database alone tracks more than 138 million lost or stolen documents~\cite{interpol2023sltd}, while identity fraud losses in the United States exceeded \$40 billion in 2023~\cite{javelin2023fraud}. Detecting and mitigating the quality impact of such covert perturbations remains an unsolved problem in \acs{FIQA}.

% \section{Problem Statement}\label{sec:problem_statement}

% Despite significant progress, current \acs{FIQA} methods are not equipped to detect subtle but security-critical degradations introduced by steganography. These pixel-level modifications are designed to evade human perception yet can severely undermine biometric feature extraction. Existing \acs{FIQA} models, whether based on handcrafted features~\cite{henniger2020biosig}, supervised learning with annotated quality labels~\cite{hernandez2019faceqnet, meng2021magface}, or ranking-based formulations~\cite{liu2017rankiqa}, are generally trained on visible distortion categories such as blur, noise, and compression. As a result, they fail to generalize to covert perturbations that are imperceptible yet operationally detrimental.

% A second limitation concerns fairness. \acs{FIQA} models often inherit demographic imbalances from their training data, yielding systematically lower scores for certain groups~\cite{babnik2022bias}. In the context of MRTDs, governed by ICAO and ISO/IEC standards~\cite{icao2015, iso2010iec}, such disparities carry serious ethical and legal consequences. At the same time, evidence from security agencies underscores the prevalence of document fraud and morphing attacks, reinforcing the urgency of robust and equitable quality control.

% To address these gaps, this thesis proposes a no-reference \acs{FIQA} framework explicitly sensitive to steganographic distortions and demographic fairness. The approach fuses multiple full-reference metrics into a proxy ground truth aligned with subjective judgments. This fused signal supervises the training of an NR-FIQA model tailored to steganographically modified facial images. By integrating perceptual validity, biometric utility, and fairness considerations, the framework aims to deliver a principled bridge between human perception, algorithmic requirements, and the security needs of modern travel documents.






























% \chapter{Introduction}\label{chap:introduction}

% \section{Contextualization}\label{sec:contextualization}

% Image quality refers to the degree to which a visual representation meets perceptual or functional expectations. It is typically associated with the presence or absence of distortions, artifacts, or degradations that affect how an image is perceived by humans or processed by machines
% %~\cite{survey2006wang, survey2009thung, shahrukh2019survey}.
% High-quality images preserve structural, textural, and color information in a way that aligns with human visual preferences or supports reliable computer vision performance~\cite{sheikh_vif_2006, zhang_lpips_2018}.

% \acl{IQA} (\acs{IQA}) is the process of quantifying image quality in a systematic and reproducible way. It plays a central role in optimizing image acquisition~\cite{mishra2017aquisition}, compression~\cite{wang_no_reference_jpeg_2002}, transmission~\cite{engelke_wireless_iqa_2010}, and restoration pipelines~\cite{dabov_bm3d_2007}. \acs{IQA} metrics aim to provide reliable quality estimates that correlate well with human perception~\cite{wang_ssim_2004, FullReference2024}. Due to the complexity of human vision and its subjective nature, building computational models that accurately reflect perceived quality remains a challenging task.

% \acs{IQA} metrics are generally categorized as subjective or objective. Subjective assessment involves human observers who rate the perceived quality of images, typically following standardized protocols such as ITU-R BT.500~\cite{itu_bt500_2023}. While subjective metrics are considered the gold standard due to their direct alignment with human perception, they are time-consuming, expensive, and not scalable. In contrast, objective metrics rely on computational models to estimate image quality automatically, aiming to approximate human judgment with high consistency and low cost~\cite{zaric_comparison_objective_subjective, sheikh_vif_2006}.

% Subjective image quality assessment relies on human evaluations to generate ground-truth perceptual scores, often in the form of \acl{MOS} (\acs{MOS}) or \acl{DMOS} (\acs{DMOS}). These scores are typically collected under controlled conditions following international standards such as ITU-R BT.500 (International Telecommunication Union ,  Raciocommunication Sector Recommendation ,  Broadcasting service -- Television~\cite{itu_bt500_2023}. Subjective assessment captures nuances of human vision that are difficult to model algorithmically, making it essential for validating and benchmarking objective \acs{IQA} models~\cite{ponomarenko_tid2013, zaric_comparison_objective_subjective}. However, it is inherently limited by inter-observer variability, cultural or demographic bias, and the logistical costs associated with large-scale studies~\cite{ghadiyaram_crowdsourced_study_2016}. Crowdsourcing platforms like Amazon Mechanical Turk~\cite{amazon_mturk} have recently enabled more scalable data collection, though at the expense of environmental control and consistency.

% Objective \acs{IQA} methods are commonly divided into three categories: full-reference (FR), reduced-reference (RR), and no-reference (NR). FR-IQA assumes access to an undistorted reference image and compares it to the distorted version using perceptual models~\cite{wang_ssim_2004, sheikh_ifc_2005, zhang_fsim_2011}. RR-IQA methods extract partial information from the reference image, enabling a compromise between performance and practicality~\cite{wang_wavelet_statistics_2005, li_divisive_normalization_2009}. NR-IQA, also known as blind \acs{IQA}, operates without any reference and is considered the most challenging, requiring models to infer quality based solely on the distorted image~\cite{mittal_brisque_2012, bosse_deep_nriqa_2018}.

% Facial Image Quality Assessment (FIQA) is a specialized subfield of \acs{IQA} that aims to quantify the quality of facial images with respect to their utility in downstream biometric or analytic tasks. Unlike general-purpose \acs{IQA}, \acs{FIQA} must account for both perceptual attributes (e.g., blur, noise) and task-specific considerations such as facial pose, occlusion, expression, and alignment~\cite{hernandez_fiqanet_2020, boutros_iqface_2021, li_biofacenet_2021}. High-quality face images are crucial for ensuring the accuracy and fairness of face recognition systems, particularly in security, forensics, and surveillance domains~\cite{xu_secureqnet_2020, luo_deepiq_2018}. Consequently, \acs{FIQA} models often incorporate features from deep face recognition networks to align quality predictions with identity discrimination performance~\cite{FaceMetric2025}. This task-oriented nature of \acs{FIQA} makes it fundamentally different from traditional perceptual quality assessment.

% \acs{FIQA} systems have been shown to produce different quality scores depending on a person's age, gender, or skin tone~\cite{jo_ifqa_2024, FaceMetric2025}. These differences often happen because the training data includes more examples from certain groups and fewer from others. For example, images of people with darker skin or non-frontal poses are often less common in training sets, which leads to lower quality scores for those individuals~\cite{kanwisher2006fusiform}. This can cause face recognition systems to perform worse for some groups than others, raising serious concerns about fairness.

% The International Civil Aviation Organization~\cite{icao_2015} (ICAO) and the ISO/IEC 19794--5 standard~\cite{iso_iec29794-5_2010} establish guidelines for image quality in Machine-Readable Travel Documents (MRTDs). These guidelines ensure uniform image conditions (e.g., lighting, focus, and resolution) and consistency across datasets. While these regulations establish a technical baseline, they do not account for perceptual biases and demographic variability in \acs{FIQA}.\@

% The ethical implications of these biases are profound. Political regulations, such as the European Convention on Human Rights (Article 14)~\cite{echr_article14}, the Universal Declaration of Human Rights (Article 7)~\cite{udhr_article7}, the General Data Protection Regulation (Article 22)~\cite{gdpr_article22}, and emerging AI governance frameworks, such as the European Artificial Intelligence Act (2024)~\cite{eu_ai_act_2024} and proposals in the USA~\cite{us_ai_bill_rights_2022}, aim to prevent discriminatory decisions. Despite these efforts, biases persist, often introduced through the human observers who evaluate facial images for \acs{FIQA} algorithms.

% Evidence from neuroscience further supports the complexity of facial image perception. The fusiform face area, a specialized region in the human brain, is selectively activated by face stimuli~\cite{kanwisher2006fusiform, tsao2008mechanisms}. This biological specialization makes \acs{FIQA} particularly sensitive to both stimulus features (e.g., age, gender, ethnicity, attractiveness) and the demographic background of the observers.

% An even greater challenge arises when evaluating the quality of steganographically distorted facial images. Steganography is the practice of concealing information within digital media, typically by subtly modifying pixel values in a way that is imperceptible to human observers~\cite{steganography}. Although visually unobtrusive, these alterations can degrade biometric features and compromise recognition performance. NR-IQA approaches, while not requiring references, are generally not designed to detect such imperceptible, task-relevant distortions.

% A particularly relevant subclass of steganography for identity documents is printer-proof steganography, which embeds information in a way that remains intact through the print-scan process~\cite{codeface2021, stegastamp2020}. These techniques aim to survive real-world transformations such as color shifts, compression, and physical degradation, while maintaining visual fidelity. However, even when such methods are visually imperceptible, they can induce subtle frequency-domain changes or spatial artifacts that disrupt face recognition systems. Assessing image quality in this context requires NR-IQA methods that are sensitive not only to perceptual degradation but also to recognition-relevant cues.


% \section{Problem Statement}\label{sec:problem_statement}

% Despite significant advances in \acs{FIQA}, current approaches struggle to capture task-specific degradations introduced by modern steganographic techniques. Existing methods often rely on handcrafted features~\cite{henniger2020biosig}, supervised quality learning~\cite{hernandez2019faceqnet, meng2021magface, terhorst2020serfiq}, or rank-based formulations~\cite{liu2017rankiqa}. However, these frameworks are typically trained on standard datasets and are not optimized to handle distortions that preserve visual fidelity while disrupting utility. Moreover, demographic and perceptual biases embedded in training data~\cite{babnik2022} raise concerns about the fairness and generalizability of \acs{FIQA} systemsn. This is especially problematic for applications involving MRTDs, where regulation mandates consistent quality but does not address ethical or perceptual variance~\cite{icao_2015, iso_iec29794-5_2010}. Therefore, there is a critical need for NR-IQA methods that can detect subtle, high-impact degradations in facial images, particularly those arising from adversarial steganography while accounting for demographic fairness and interpretability.

% Traditional \acs{IQA} frameworks tend to generalize across domains, but real-world applications demand task-aware assessment strategies. In the context of facial biometrics, image quality must be evaluated not only in terms of visual fidelity but also in terms of its impact on recognition performance and fairness. This motivates the development of application-specific \acs{IQA} pipelines that consider context-dependent factors, such as demographic variability and task utility. At the same time, the perceptual dimension of image quality remains central. Human observers are still used as the ground truth in subjective assessments~\cite{ITU-R-BT500, mos2016}, yet NR-IQA models often diverge from these judgments, especially in complex scenarios involving subtle degradations or diverse populations. To bridge this gap, new strategies are needed to align NR-IQA with subjective perception, either by integrating perceptual priors, modeling bias sources, or learning from pseudo-MOS annotations that reflect human preferences~\cite{chen2021pseudo, jin2020pipal}.

% To address the limitations of existing approaches, we propose a NR-IQA framework tailored to facial images affected by subtle or task-relevant distortions, such as those introduced by steganography. Our method relies on a fusion strategy that combines multiple FR metrics, trained using supervised regression against a curated set of subjective scores. This fused metric serves as a proxy ground truth to guide the learning of a NR-IQA model, enabling it to approximate perceptual quality without requiring access to pristine reference images. The framework is evaluated across a steganographically augmented dataset of facial images, incorporating multiple embedding levels and distortion types. By integrating perceptual fidelity, recognition utility, and robustness to imperceptible alterations, the proposed approach seeks to bridge the gap between unbiased human perception and automatic assessment.
